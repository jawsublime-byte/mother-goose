interface:
  display_name: "The Emperor's New Clothes"
  short_description: "Verify independently instead of inheriting consensus."
  default_prompt: "Use $emperor-new-clothes to verify this result from direct evidence only."

policy:
  allow_implicit_invocation: false
